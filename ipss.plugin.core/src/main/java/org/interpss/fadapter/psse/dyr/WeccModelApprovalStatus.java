package org.interpss.fadapter.psse.dyr;

/** WECC disposition copied from a versioned approved-model-library row. */
public enum WeccModelApprovalStatus {
    APPROVED,
    RETIRED,
    UNAPPROVED,
    NEVER_APPROVED,
    TRANSITIONAL
}
